import UserDashboard from "../directoryEntities/user/UserDashboard";

const UserDirectoryPage = () => {
	return (
		<div style={{ height: "100%" }}>
			<UserDashboard />
		</div>
	);
};

export default UserDirectoryPage;
